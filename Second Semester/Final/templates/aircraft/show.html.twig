{% extends 'base.html.twig' %}

{% block title %}Aircraft{% endblock %}

{% block body %}
    <h1>Aircraft</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aircraft.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ aircraft.model }}</td>
            </tr>
            <tr>
                <th>nation_manufacturer</th>
                <td>{{ aircraft.getNationManufacturer()}}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_aircraft_index') }}">back to list</a>

    <a href="{{ path('app_aircraft_edit', {'id': aircraft.id}) }}">edit</a>

    {{ include('aircraft/_delete_form.html.twig') }}
{% endblock %}
