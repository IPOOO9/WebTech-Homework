{% extends 'base.html.twig' %}

{% block title %}Edit Battles{% endblock %}

{% block body %}
    <h1>Edit Battles</h1>

    {{ include('battles/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_battles_index') }}">back to list</a>

    {{ include('battles/_delete_form.html.twig') }}
{% endblock %}
