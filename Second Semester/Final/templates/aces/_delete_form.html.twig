<form method="post" action="{{ path('app_aces_delete', {'id': ace.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ace.id) }}">
    <button class="btn">Delete</button>
</form>
