{% extends 'base.html.twig' %}

{% block title %}Nations{% endblock %}

{% block body %}
    <h1>Nations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nation.id }}</td>
            </tr>
            <tr>
                <th>Nation_name</th>
                <td>{{ nation.NationName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nations_index') }}">back to list</a>

    <a href="{{ path('app_nations_edit', {'id': nation.id}) }}">edit</a>

    {{ include('nations/_delete_form.html.twig') }}
{% endblock %}
